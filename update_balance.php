<?php
// Получаем значение score из POST-запроса
$score = $_POST['score'];

// Получаем текущий общий баланс пользователя (например, из базы данных или файла)
$currentBalance = 0; // Здесь нужно использовать ваш способ получения текущего баланса

// Прибавляем очки из игры к общему балансу
$newBalance = $currentBalance + $score;

// Сохраняем новое значение баланса (например, в базу данных или файл)
// Здесь нужно использовать ваш способ сохранения нового баланса

// Возвращаем обновленное значение баланса в формате JSON
$response = ['balance' => $newBalance];
header('Content-Type: application/json');
echo json_encode($response);
?>
